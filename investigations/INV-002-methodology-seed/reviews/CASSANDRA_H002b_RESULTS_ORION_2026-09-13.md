# CASSANDRA — H002b RESULTS (ORION priority · QUANT skeptic flag first-class)
**Memo:** `experiments/results/H002b_EXPLORATORY_RESULTS_2026-09-13.md`  
**Prior:** `CASSANDRA_H002b_RESULTS_REDTEAM_2026-09-13.md`  
**Date:** 2026-09-13  

---

## Direct answers

| Question | Answer |
|----------|--------|
| Is protocol SURVIVES **valid but hollow**? | **Yes.** Numerically meets locked gates vs hard one-hot IS-majority log-loss. Economically / scientifically **hollow** — lift≈18 is baseline pathology, not precursor skill. |
| Does soft empirical-prior kill the claim? | **Yes.** Descriptive soft-prior lift≈0.017 CI crosses 0 → **no** decision-grade lift. Bal-acc≈0.37 / acc≈0.54 vs maj≈0.48 = weak discrimination. |
| Require H002c with soft prior as primary? | **Yes, if** org wants a log-loss SURVIVES path later — on **unseen** weeks. **This** run should **not** be re-labeled SURVIVES after swapping baseline post-peek. |
| Ban strong-edge prose? | **Yes — mandatory.** |
| Agree with SURVIVES decision class? | **Disagree for board / intelligence.** Agree only as “mechanical pass vs strawman baseline.” |

---

## Recommended board language (locked)

**H002b = FAILS (predictive claim) / SURVIVES-vs-strawman only (non-promotable).**

Preferred single board line:

> **H002b FAILS as a precursor classifier:** soft constant (IS empirical prior) log-loss lift is ≈0 with CI through 0; balanced accuracy ≈0.37. Protocol “SURVIVES” vs hard one-hot majority is a **mechanical artifact** (L_base≈18.8 from clipped zeros) and is **not** board SURVIVES. PARAMETER 7–9 classifier is **not a panacea** (005). No Observed class identity. No MERCURY.

Alternate two-line if audit trail must mention protocol gates:

> 1. Mechanical protocol gates vs hard IS-majority one-hot: pass (hollow).  
> 2. **Board decision: FAILS** — no lift vs soft empirical-prior constant; weak bal-acc.

---

## Attack summary (QUANT flag = first-class)

### CRITICAL — Hard one-hot + clipped log-loss
Constant predictor with p=1 on `high`, p=0 else → huge loss on ~52% OOS days ≠ high. Soft model almost any calibrated probs “SURVIVES.” Persistence shows **same** inflated lift → smoking gun.

### HIGH — Soft prior kills claim
Empirical-prior L_base≈0.766 vs L_model≈0.749 → lift≈0.017 CI includes ≤0. That is the correct constant baseline for multiclass log-loss.

### HIGH — Discrimination weak
Bal-acc 0.373 ≈ 3-class chance; accuracy barely above majority rate.

### MEDIUM — Rare compression OOS n=8
Unstable cell; does not create skill.

### Packaging lesson
“IS-majority constant predictor” was underspecified for **log-loss** (hard label vs soft prior). H002c should lock: **PRIMARY baseline = IS empirical distribution over {high,low,neither}.** Hard majority accuracy can stay descriptive.

---

## H002c (only if re-opening)

1. NEW id.  
2. Primary baseline = soft IS class frequencies.  
3. Same T*/y/thresholds/hash unless explicitly re-frozen.  
4. **Decision sample = weeks/days not used as OOS in this memo** (or accept this memo as permanent FAILS under soft prior and only use H002c for future tape).  
5. Ban promoting this run’s hard-baseline lift.

---

## Vocab (confirm)

| Allowed | Banned |
|---------|--------|
| PARAMETER classifier / precursor | Observed trending/consolidating/reversing confirmed |
| **not a panacea** (005) | Strong edge / crystal-ball / SURVIVES without strawman caveat |
| FAILS (board) | MERCURY / trade permission |
| Hollow mechanical pass vs one-hot (audit only) | Bare “H002b SURVIVES” on the board |

---

## Path

`reviews/CASSANDRA_H002b_RESULTS_ORION_2026-09-13.md`
